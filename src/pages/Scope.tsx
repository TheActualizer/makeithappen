import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const Scope = () => {
  return (
    <div className="container mx-auto p-4">
      <Card>
        <CardHeader>
          <CardTitle>Scope</CardTitle>
        </CardHeader>
        <CardContent>
          <p>Scope content will go here</p>
        </CardContent>
      </Card>
    </div>
  );
};

export default Scope;